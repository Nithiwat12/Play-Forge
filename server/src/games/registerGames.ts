// Central place that wires every implemented game into the GameRegistry.
// Adding a future game means adding one line here (plus its DB row) -
// nothing else in the platform needs to change.
import "./spyfall";
import "./wordhead";
import "./island_betrayal";

export {};
