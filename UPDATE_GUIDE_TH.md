# GameCen — แก้กลับเข้าเกมค้างและลดงานซ้ำ

## วิธีนำไปใช้

1. สำรองโปรเจกต์เดิมไว้ แล้วแตก ZIP นี้ในโฟลเดอร์ใหม่
2. นำ `server/.env` และ `client/.env` จากเครื่องคุณมาใส่ในโฟลเดอร์ใหม่ หรือกรอกค่าตาม `.env.example`
   ZIP นี้ไม่รวมรหัสลับ, node_modules, .git หรือผล build เก่า
3. เปิด Terminal ในโฟลเดอร์ GameCen แล้วรัน:

```sh
npm ci --prefix server --include=dev
npm ci --prefix client --include=dev
npm run prisma:generate
npm run prisma:deploy --prefix server
npm run build:server
npm run build:client
npm test
```

4. สำหรับเครื่องพัฒนา เปิด Terminal สองหน้าต่างแล้วรัน `npm run dev:server` และ `npm run dev:client`
5. สำหรับเว็บที่อยู่บน Render ให้อัปเดตโค้ดทั้ง client และ server ไปยัง repository เดิมแล้ว deploy ทั้งคู่
   `render.yaml` เดิมมีคำสั่ง migrate deploy อยู่แล้ว ให้รอฝั่ง server พร้อมก่อนเปิดใช้ frontend รุ่นใหม่
6. ให้ผู้เล่นรีเฟรชหน้าเว็บหลังอัปเดต หลีกเลี่ยงอัปเดตระหว่างรอบที่กำลังเล่น

**ต้องอัปเดตทั้ง client และ server**: รูปแบบคำตอบ `room:join` เพิ่ม `gameState` และมี event `room:pause` ใหม่
ไม่ต้องลบฐานข้อมูลหรือใช้คำสั่ง reset; migration ใหม่เพิ่มดัชนีเท่านั้น

## สิ่งที่แก้

- กลับเข้าเกม: รับ room และ public/private state ใน acknowledgement เดียวกัน
  จึงไม่ต้องรอผู้เล่นคนอื่นทำ action เพื่อให้ข้อมูลเกมมาถึง
- ป้องกัน loading ไม่สิ้นสุด: มีเวลารอเชื่อมต่อ 12 วินาที, acknowledgement 8 วินาที,
  HTTP request 15 วินาที พร้อมข้อความผิดพลาด ปุ่มลองใหม่ และกลับหน้าประวัติ
- หากห้อง PLAYING ไม่มี engine จะตอบ error ชัดเจน; ห้อง FINISHED จะไม่ส่งเข้า PlayPage
- ออกจากหน้าเกมใช้ `room:pause` เก็บที่นั่ง บทบาท และคะแนนเดิมไว้
  กลับจากหน้าประวัติแล้วเล่นต่อได้ ส่วนออกจากล็อบบี้ยังเป็นการออกจากห้องจริง
- รองรับที่นั่งที่ถูกโค้ดรุ่นเดิมระบุว่าออกแล้วระหว่าง PLAYING เฉพาะผู้เล่นใน roster ของรอบนั้น
  ผู้เล่นใหม่ที่ไม่ได้อยู่ในรอบไม่สามารถเข้ากลางเกมได้
- เชื่อมต่อใหม่แล้วกู้สถานะ connected ของผู้เล่น พร้อมเพิกเฉยต่อคำตอบเก่าหลังเปลี่ยนหน้า
- ใช้ Socket เดิมซ้ำเมื่อเชื่อมต่อไม่สำเร็จ เพื่อรักษา listeners และปฏิเสธ action ตอน offline
- หนึ่ง Socket สมัครรับข้อมูลได้ทีละห้อง และ game:state มี roomId ป้องกันข้อมูลห้องเก่าปน
- ไม่ logout ผู้ใช้เพียงเพราะ /auth/me ล่มชั่วคราว; ยัง logout เมื่อ API ตอบ 401
- ยุบห้องแล้วหยุด timer/listeners ของ engine โดยไม่เรียกการให้คะแนนตามปกติ
- ระบบล้างห้องเก่าข้ามล็อบบี้ที่ยังมีผู้ใช้เชื่อมต่ออยู่
- ปุ่มออก/ยุบห้องรอเซิร์ฟเวอร์ยืนยันและแสดงข้อผิดพลาดได้
- แก้ server/package-lock.json ที่ไม่ตรงกับ dependencies เพื่อให้ npm ci ใช้ได้

## การปรับความเร็ว

- หน้าประวัติเดิมดึง ID ห้อง แล้วเรียกโหลดรายละเอียดเพิ่มทีละห้อง
  เมื่อมี 20 ห้อง = 1 ครั้งสำหรับรายการ + 20 ครั้งสำหรับรายละเอียด
  รุ่นนี้โหลด relations รวมผ่าน Prisma findMany ครั้งเดียวต่อรายการ
  (Prisma อาจสร้าง SQL มากกว่าหนึ่งคำสั่งภายใน จึงไม่อ้างว่าเหลือ SQL เดียว)
- ดึงข้อมูลผู้เล่นเฉพาะ id/username ที่ใช้จริง แทนการอ่านข้อมูลบัญชีทั้งแถว
- โหลด sessions และ players สำหรับ scoreboard พร้อมกันด้วย Promise.all
- เพิ่มดัชนี rooms(status, updatedAt), room_players(userId, leftAt),
  game_sessions(roomId, status, startedAt)
- รักษา route lazy loading, React memoization และ gzip ที่โปรเจกต์เดิมมีอยู่แล้ว
  สิ่งเหล่านี้ไม่ได้เพิ่มใหม่ในรุ่นนี้
- ไม่ cache private state หรือข้อมูลห้องที่เปลี่ยนตลอด เพราะอาจทำให้บทบาท/สถานะเก่าปรากฏ

ยังไม่มี benchmark บนฐานข้อมูล/เครือข่ายจริง จึงไม่ระบุเปอร์เซ็นต์ความเร็วที่เพิ่มขึ้น
ผลที่ยืนยันจากโค้ดได้คือการตัดการโหลดรายละเอียดห้องซ้ำ และลดข้อมูลบัญชีที่อ่าน

## เมื่อเซิร์ฟเวอร์รีสตาร์ต

เกมเดิมอยู่ใน RAM และไม่ได้บันทึก snapshot ระหว่างเล่น จึงกู้รอบเดิมข้ามการรีสตาร์ตไม่ได้
รุ่นนี้ก่อนเปิดรับการเชื่อมต่อ จะทำ transaction เพื่อ:

- ปิด session IN_PROGRESS ที่ค้างเป็น ABORTED
- ล้าง ready ของผู้เล่นในห้องที่ค้าง PLAYING
- เปลี่ยนห้องเหล่านั้นเป็น WAITING ให้เริ่มรอบใหม่ได้

คะแนนของรอบที่จบแล้วคงอยู่; รอบที่ถูกขัดจังหวะจะไม่มีการให้คะแนน
**ระบบยังรองรับ server เพียงหนึ่ง instance ต่อฐานข้อมูล** ตามสถาปัตยกรรมเดิม
ห้าม scale เป็นหลาย instance โดยใช้ startup recovery นี้ร่วมกัน
การรองรับหลาย instance/กู้เกมเดิมหลัง restart ต้องเพิ่ม shared state และ lifecycle ownership ก่อน

## ผลตรวจสอบ

- Client production build: ผ่าน (TypeScript + Vite)
- Server TypeScript build: ผ่าน
- npm test: 8 tests ผ่าน
  - Client lifecycle 5 tests: snapshot ใน ack, ไม่เคย connect, reconnect,
    cleanup/StrictMode และ join/connect error
  - Server 3 tests: ใช้ Socket.IO จริงกับ engine จริงทดสอบ pause/rejoin,
    state หาย, ห้องปิด, รักษาบทบาทเดิม; ตรวจ legacy rejoin/ปฏิเสธผู้เล่นใหม่;
    ตรวจ abort ไม่ส่ง event ให้คะแนน
- Tests ฝั่ง server ใช้ mock เฉพาะการอ่าน/เขียนฐานข้อมูล และกำหนด URL ทดสอบเอง
- Prisma Client generation ใช้ engine ที่มีในสภาพแวดล้อมทดสอบ; ไม่ได้รัน migration กับฐานข้อมูลจริง
- ไม่ได้ทดสอบผ่าน browser บนเว็บจริง, ฐานข้อมูลจริง, cold start ของ hosting หรือ load test หลายผู้เล่น

## ตรวจหลัง deploy

ใช้บัญชีอย่างน้อย 3 คน: สร้างห้อง → พร้อม → เริ่มเกม → จดบทบาทของตนเอง → ออกจากเกม
→ ประวัติ → กลับเข้าเกม แล้วตรวจว่าบทบาทและรอบเดิมยังอยู่
ลองตัด/ต่ออินเทอร์เน็ต และทดสอบยุบห้อง จากนั้นตรวจว่ามีข้อความ/ทางกลับเมื่อเข้าไม่ได้
การ restart จะกลับล็อบบี้ตามพฤติกรรมที่อธิบายข้างต้น ไม่ใช่เล่นรอบเดิมต่อ
