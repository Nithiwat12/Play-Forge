# เพิ่มเกม ito — ใจตรงกันไหม?

เพิ่มเกมใหม่ใน GameCen จากแนวการเล่น ito ที่ปรากฏในวิดีโอที่แนบ โดยใช้ UI และชุดหัวข้อที่เขียนขึ้นใหม่ ไม่ใช้ภาพหรือเสียงจากคลิป

## รูปแบบการเล่น
- นั่งด้วยกัน: พูดคำใบ้ได้ ไม่บังคับพิมพ์ ใช้อุปกรณ์และบัญชีของแต่ละคนดูเลขลับและยืนยันเปิดไพ่
- ออนไลน์ไม่ใช้เสียง: พิมพ์คำใบ้ของไพ่ทุกใบ ดูคำใบ้ของเพื่อน และใช้ปุ่มเสนอเปิด / เห็นด้วย / รอก่อน ไม่ต้องใช้ไมโครโฟน
- รองรับ 2–8 คน เลือก 1–3 ด่าน และเวลา 2–20 นาทีต่อด่านตอนสร้างห้อง

## กติกาของเวอร์ชันที่พัฒนา
1. ผู้เล่นอยู่ทีมเดียวกัน เริ่มด้วยหัวใจร่วม 3 ดวง
2. สุ่มเลข 1–100 ไม่ซ้ำกัน เริ่มไพ่คนละใบ ผ่านด่านแล้วเพิ่มคนละใบ
3. ระบบสุ่มหัวข้อใหม่พร้อมคำอธิบายปลายสเกลต่ำและสูง
4. ใบ้เป็นตัวอย่างตามหัวข้อ ห้ามบอกเลขลับหรือรหัสแทนเลข กรองตัวเลขอารบิกและไทยในข้อความ แต่การบอกเลขเป็นคำหรือรหัสต้องอาศัยกติกาที่ผู้เล่นตกลงกัน
5. เสนอเปิดได้เฉพาะไพ่ของตน ทุกคนยืนยันจึงเปิดได้ ผู้เสนอถือว่ายืนยันแล้ว ใครไม่แน่ใจเลือก “รอก่อน” เพื่อยกเลิกข้อเสนอ
6. เปิดจากน้อยไปมาก ถ้าข้ามไพ่ต่ำกว่า เสียหัวใจ 1 ดวงต่อการเปิดผิดครั้งนั้น และนำไพ่ต่ำกว่าที่ถูกข้ามออกพร้อมแสดงเลข
7. ไพ่หมดแล้วทุกคนกดพร้อมเพื่อไปด่านถัดไป (รอได้ 3 นาที)
8. ผ่านทุกด่านชนะร่วมกัน หมดหัวใจหรือหมดเวลาแพ้ บันทึกผลทีมในประวัติ
9. ผู้เล่นหลุด: เก็บเลขและคำใบ้ไว้ ยกเลิกข้อเสนอค้าง และรอกลับเข้าห้อง เวลาด่านยังเดินต่อ ไม่เปิดไพ่แทนผู้เล่นที่หลุด
10. จบเกมกลับล็อบบี้ กดพร้อมแล้วหัวหน้าห้องเริ่มเกมใหม่

หัวใจ การจัดการไพ่ที่ถูกข้าม การยืนยันพร้อมกันและเวลาเป็นกติกาที่ระบุชัดสำหรับเวอร์ชันเว็บนี้ ไม่อ้างว่าเหมือนเกมต้นฉบับทุกฉบับ

## ติดตั้ง / อัปเดต
นำไฟล์ source ไปทับโปรเจกต์เดิม โดยเก็บ `.env` เดิมไว้เอง ZIP นี้ไม่รวม node_modules, build, .git หรือค่าลับ

รันจากโฟลเดอร์ GameCen:

```powershell
npm run install:all
npm run prisma:generate
npm run prisma:deploy --prefix server
npm run build:server
npm run build:client
```

Migration ใหม่เพิ่มรายการเกม `ito` โดยไม่แก้ตารางเดิมและไม่เพิ่มรายการซ้ำ ไม่จำเป็นต้องรัน seed สำหรับฐานข้อมูลเดิม
สำหรับฐานข้อมูลใหม่สามารถใช้คำสั่ง migrate deploy ข้างต้นตามด้วย `npm run seed`

เปิดสอง terminal:

```powershell
npm run dev:server
```

```powershell
npm run dev:client
```

เข้าเมนูเกม → ito → สร้างห้อง → เลือกรูปแบบ → ให้เพื่อนเข้าห้องด้วยบัญชีของตัวเอง → กดพร้อม → หัวหน้าห้องเริ่มเกม
หากเกมไม่ปรากฏ ตรวจว่าได้ deploy migration บนฐานข้อมูลที่เซิร์ฟเวอร์ใช้อยู่ และ restart backend แล้ว

## สิ่งที่เปลี่ยน
- เพิ่ม engine ito, ชุดหัวข้อ 12 หัวข้อ, การตรวจค่า settings และการป้องกันเลขลับฝั่งเซิร์ฟเวอร์
- เพิ่มหน้าเกม responsive, ตัวเลือกโหมด/ด่าน/เวลา, วิธีเล่น, ไอคอน และผลทีมในหน้าประวัติ
- ลงทะเบียนเกมทั้ง client/server เพิ่ม seed และ migration
- เพิ่ม unit และ Socket.IO integration tests: `npm run test:ito --prefix server`
- อัปเดตล่าสุด: ลบ Island Betrayal และแยกโหมด Spyfall/WordHead แล้ว ดู ALL_MODES_UPDATE_TH.md

## ขอบเขตการตรวจสอบ
- TypeScript client และ server ผ่าน
- เกม ito: 7 tests กติกา/ความเป็นส่วนตัว/validation/timeout และ 2 tests Socket.IO จริง แยก TABLE กับ ONLINE ผ่าน
- Socket.IO tests ครอบคลุมเข้าห้อง เริ่มเกม จำกัดสิทธิ์ ไพ่เฉพาะเจ้าของ โหวต หลุด/กลับเข้า และบันทึกผลหนึ่งครั้ง โดยจำลองฐานข้อมูลในหน่วยความจำ
- regression เดิม Island Betrayal 16 tests รวม Socket.IO 4/6/8/10/12/15 คนผ่าน
- ทดสอบ client reconnect เดิม 5 tests ผ่าน และ Vite production build ผ่าน
- ยังตรวจภาพหน้าจอใน browser ไม่ได้ เพราะ runtime ไม่มี browser และดาวน์โหลดไม่สำเร็จ
- ยังไม่ได้เชื่อมต่อหรือรัน migration บนฐานข้อมูลจริงของผู้ใช้
- สถานะเกมกำลังเล่นยังอยู่ในหน่วยความจำของ server ตามระบบเดิม: reconnect ได้เมื่อ process เดิมยังทำงาน แต่ restart server ไม่กู้เกมที่กำลังเล่น

## ข้อความ commit ที่แนะนำ

```text
Add ito cooperative number game with table and online modes

- Add private number cards, original topics, team hearts and progressive stages
- Require unanimous card reveals and validate stale/duplicate actions
- Add typed clues, room settings, reconnect handling and team history
- Register ito catalog migration and seed entry
- Add engine and Socket.IO integration coverage for both play modes
```
